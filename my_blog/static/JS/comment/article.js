import {
            myVariables,
            searching,
            send2,
            nono,
            buttons,
            parseNameToNumber,
            submitData,
            getCookie,
            send,
            work,
            addHtmlComment,
            cleanDataForm,
            addHtmlReply,
            send3,
            dateLocalForComment
        } from './do-comment.js';

// We put the amount of star
nono();
// We put the click event inside the element with id commentary
work('unique');
// We load the click event inside with the send function
searching('search');

//
addHtmlReply();
//
dateLocalForComment('js-comment-date');